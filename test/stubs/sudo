#!/usr/bin/env bash
# Test stub: run the command directly, the test does not need root
set -euo pipefail

while [[ $# -gt 0 ]]; do
    case "$1" in
        -*) shift ;;
        *) break ;;
    esac
done

exec "$@"
